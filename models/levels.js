// SELECT id, label
// FROM tcselles.levels;

// INSERT INTO tcselles.levels
// (label)
// VALUES('');

// UPDATE tcselles.levels
// SET label=''
// WHERE id=0;

// DELETE FROM tcselles.levels
// WHERE id=0;
