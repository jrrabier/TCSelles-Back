// SELECT users_id, meetings_id, status, is_playing
// FROM tcselles.users_meetings;

// INSERT INTO tcselles.users_meetings
// (users_id, meetings_id, status, is_playing)
// VALUES(0, 0, 0, 0);

// UPDATE tcselles.users_meetings
// SET status=0, is_playing=0
// WHERE users_id=0 AND meetings_id=0;

// DELETE FROM tcselles.users_meetings
// WHERE users_id=0 AND meetings_id=0;
